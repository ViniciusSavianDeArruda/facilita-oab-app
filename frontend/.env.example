# Só necessário em produção quando o front e o back NÃO estão no mesmo
# domínio (ex.: front na Vercel, back no Fly.io). Em dev local, deixe
# vazio/comentado — o proxy do Vite (vite.config.js) cuida do /api.
# VITE_API_URL=https://seu-backend.exemplo.com
